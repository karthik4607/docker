---
- name: Copying Dockerfile
  hosts: localhost
  become: true

  tasks:
    - name: Copy file from 
      ansible.builtin.copy:
        src: /home/ktk/mongo/docker/Dockerfile
        dest: /tmp/Dockerfile  
        remote_src: yes 
          
